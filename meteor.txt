Instalacion de meteorSe debe tener Node JS instalado

Ejecutar en una consola
Windows mediante Power Shell
npm install -g meteor --release 2.3.1

C:\> choco install meteor --params="'/RELEASE:1.9.3'"
Esta version solo instala 0.5

Linux MAC
curl "https://install.meteor.com/?release=2.1" | sh



Para tomar la version sobre la que está construida la app

import { Meteor } from 'meteor/meteor'

Meteor.release



Ejecutar meteor desde las fuentes

 meteor run --settings settings/settings.json --port 3000